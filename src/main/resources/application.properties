# Server configuration (use Render's $PORT if provided)
server.port=${PORT:8080}

# MongoDB Atlas configuration (read from env var `SPRING_DATA_MONGODB_URI`)
# Example fallback uses the current value but it's recommended to set the env var in Render
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://admin:admin@awd.ypmipjt.mongodb.net/Products?retryWrites=true&w=majority}

# Thymeleaf configuration (disable cache in development to see changes)
spring.thymeleaf.cache=false
